<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.yunyi.admin.dao.UmsRoleDao">

    <select id="getResourceListByRoleId" resultType="com.cy.yunyi.model.UmsResource">
        SELECT
        r.id id,
        r.create_time createTime,
        r.`name` `name`,
        r.url url,
        r.description description,
        r.category_id categoryId
        FROM
        ums_role_resource_relation rrr
        LEFT JOIN ums_resource r ON rrr.resource_id = r.id
        WHERE
        rrr.role_id = 5
        AND r.id IS NOT NULL
        GROUP BY
        r.id;
    </select>
</mapper>